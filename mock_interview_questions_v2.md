# 🎯 12306 逐字句深挖面试题库（简历原句逐条追问版）

下面按你简历里的原句逐条追问，模拟面试官连续深挖场景。重点覆盖架构取舍、实现细节、数据口径与边界条件，避免“只会背术语”。

---

## ☕ 模块零：破冰、项目真实性与日常沟通（抗压测试）
1. **基本盘问**：毕业届次、专业基础课（比如操作系统、计算机网络）掌握得怎样？实习时长及最快到岗时间？
2. **项目真实性拷问**：你简历里这两个 12306 项目，是借鉴的开源项目、培训班项目（比如马哥/尚硅谷的），还是你自己从头敲代码做出来的？你具体负责了哪几块？
3. **日常与抗压探底**：老家在哪？心仪的 base 地是哪里？因为什么选开发这条路？平时有什么爱好？运动坚持了多久？（*常见于综合面沟通题*）

---

## 🚄 项目一：12306 高并发售票核心网关（后端架构开发）

### 🔍 简历原句 1：“模拟 12306 售票场景下的高性能微服务网关。针对瞬时爆发流量，重点攻克...保障了万级并发压测下的系统稳定与数据零偏差。”
1. **网络与架构**：你提到的高性能微服务网关，是基于 Spring Cloud Gateway 还是基于 Netty 自己手写的网关？从用户浏览器发送请求，经过网关再到业务服务的全生命周期路由转发链路是怎样的？
2. **压测细节**：这里的“万级并发压测”指并发线程数还是稳定 QPS？峰值/稳态分别是多少？你是怎么压出来的，用了什么工具（JMeter/LoadRunner）？压测机器规格（几核几G）是多少？
3. **对账证明**：“数据零偏差”你是如何得出结论的？是有对账脚本（含口径与公式）还是主要基于日志审计？统计窗口和样本范围是什么？

### 🔍 简历原句 2：“基于 Cache Aside 模式构建 Caffeine + Redis 二级缓存架构以降低数据库 QPS 指标；引入 Redisson 布隆过滤器拦截非法车次请求，防止缓存穿透；利用 RLock 结合 DCL 双重检查锁解决冷启动下的并发击穿问题。”
1. **二级缓存配合**：Caffeine 是应用内缓存，Redis 是分布式缓存。一波流量打过来，这两层缓存的查询先后顺序是什么？如果 Redis 和 MySQL 数据产生了不一致，你怎么兜底收敛？降低数据库 QPS 具体降了多少（是否有 A/B 实测）？
2. **布隆过滤器穿透**：布隆过滤器的拦截逻辑具体加在代码的哪个环节？布隆过滤器有误判率（假阳性）时会放过一部分非法请求，你如何兜底？如果是临时新增车次，如何保证不被误拦（初始化与增量更新怎么做）？
3. **DCL击穿防御**：“RLock + DCL 双重检查锁”在代码层面到底是怎么写的，什么叫**双重检查**？去掉第一重或第二重检查分别会引发什么问题？冷启动时锁 Key 粒度怎么设计？

### 🔍 简历原句 3：“应用 Redis Lua 脚本实现库存原子扣减，规避数据库行锁性能瓶颈；设计分段库存 (Segmented Stock) 多 Hash 节点分摊热点流量；引入 @Transactional 保障退票写入库与 MySQL/Redis 双端库存回补的一致性；构建临界区并发碰撞单测，并在全链路压测中保障零超卖。”
1. **Lua与底仓**：为什么非要在 Redis 里面写 Lua 脚本扣减，如果纯用 Redis 的 Pipeline 或者 `decr` 命令不行吗？数据库行锁会有多大的性能瓶颈？
2. **分段方案死抠**：什么是分段库存多 Hash 节点？假设原本“G101”的 100 张票被分成了 10 个 Hash 槽（Bucket），每个槽 10 张票，如果有个用户刚好随机访问到第 5 个槽并且没票了，但其实第 6 个槽还有票，他会被直接误判为“无票”吗？
3. **@Transactional底层**：退票补偿加了 `@Transactional`，它能回滚 Redis 吗？如果不能，回补策略是什么？`@Transactional` 失效的典型场景有哪些？
4. **单测实施**：这句里的“**临界区并发碰撞单测**”，你在代码层面如何构建并发冲撞（CountDownLatch 还是 CyclicBarrier）？它验证的是“算法级不超卖”还是“全链路不超卖”？

### 🔍 简历原句 4：“采用 RocketMQ 事务消息实现业务解耦；基于自定义TransactionListener串联 Redis 预扣减与订单落库，并提供宕机补偿回补与本地事务回查兜底机制。”
1. **事务消息三阶段**：详细说说 RocketMQ 事务消息是怎么做三阶段提交的（Half消息怎么发的，何时Commit/Rollback）？
2. **本地回查与残余清理**：当出现极端网络故障，Broker 收不到提交状态而触发 `checkLocalTransaction`。如果查库一直超时或返回 UNKNOWN，如何避免事务长期悬挂？死信订单如何清理与补偿？

### 🔍 简历原句 5：“基于 Sentinel 实现核心接口的限流与超时熔断；实现全局统一返回值与异常捕获网关；构建 Daemon 异步线程池替代裸线程，避免并发下的内存溢出风险。”
1. **限流配置**：你的 Sentinel 限流是加在抢票方法上的还是整体接口上的？用了滑动窗口还是令牌桶算法？
2. **全局异常怎么抓**：对于底层抛出的 `SQLException` 或空指针，你如何在入口层统一捕获并封装为 JSON 返回（例如 `@RestControllerAdvice`）？
3. **线程参数与OOM**：为什么叫 Daemon（守护进程）异步线程池？裸线程（`new Thread()`）并发怎么就引起内存溢出了？你的线程池 `corePoolSize` 和任务队列究竟配了多少？

---

## 🤖 项目二：12306 智能数字员工 (Reactive / RAG)

### 🔍 简历原句 6：“基于 12306 业务构建的响应式多智能体协作系统，具备万级并发下的流式查票、规章问答与防攻击能力。”
1. **系统全貌**：为什么不用单体 MVC 而是要做响应式系统？从前端发起问答到送进大模型，整个项目的核心业务数据流行进路线是怎样的？
2. **防攻击深挖**：简历里的“防攻击”具体指什么？是网络层刷量，还是大模型提示词注入（Prompt Injection）？分别如何防护？

### 🔍 简历原句 7：“基于 AiServices 与 Query Router 构建多专家协作体系；实施 Session 前缀分区隔离与实体桥接 (Entity Bridging) 方案，有效解决多 Agent 协作中的历史记忆污染，实现复杂购票指令的‘隔而不离’与准确执行。”
1. **专家联调**：一个庞大复杂的 System Prompt 解决不了复杂场景吗？为什么要搞“多专家协作”和 Query Router？
2. **记忆污染**：多 Agent 协作为什么会产生“历史记忆污染”？你如何识别并量化这个问题？
3. **Session 隔离**：Session 隔离时是串行流转还是借用什么方案存储状态的？前驱的闲聊专家是怎么通过**“实体桥接”**把必要的车票实体信息安全带给购票专家的？

### 🔍 简历原句 8：“基于 Spring WebFlux + Project Reactor 重构 AI 交互链路...利用 Sinks.Many 技术实现非阻塞、背压可控的实时流式推送；集成 Redis 分布式语义缓存降低了重复请求成本。”
1. **流式原理**：跟大模型的交互，如果它生成的字符是非常巨大的，到前端是**切块下发**还是一次性等待结束下发（SSE 原理）？相比 Spring MVC，非阻塞流为什么能抗极高并发？
2. **背压防爆**：当模型产出速度远高于客户端消费速度时会发生什么？`Sinks.Many` 如何配合背压策略避免内存堆积？
3. **语义缓存拦截**：如何判断两个问题语义等价？如果异常文本（如限流报错）被写入缓存，如何避免污染后续命中？

### 🔍 简历原句 9：“针对长文本规章采用 Parent-Child 双层切片策略保障检索连贯性；集成 Milvus 与原生 BM25 双路召回，应用 RRF 算法与 Cross-Encoder 重排模型解决大模型业务幻觉痛点。”
1. **核心幻觉治理**：如何发现并解决大模型在业务当中的幻觉问题？项目里具体怎么设计**提示词卡边界**去减少幻觉？
2. **切片机制**：对于长文本为什么不传整段而是 Parent-Child 切片？如果我问关于整本规章的汇总，它是单独提取全章节还是逐节解决的？
3. **排位机制与大模型选型**：Milvus 与 BM25 打分口径不同，如何融合（RRF 公式或思想）？选择 GLM-4 的依据是什么（效果、成本、时延、稳定性）？后续还在探索哪些能力扩展？

### 🔍 简历原句 10：“实现‘短期滑动窗口 + 长期摘要压缩’双层记忆架构，防丢且突破Token 限制；引入 RAGAS 评估框架，利用 LLM-as-a-Judge 机制对召回率等核心维度进行量化评估。”
1. **摘要压缩**：当你对用户的长期聊天进行摘要压缩时，万一关键业务参数“订单号 G123”给压缩丢了，后面怎么购票？这个难点怎么解决的？
2. **评估体系落地**：在 `LLM-as-a-Judge` 方案下，RAGAS 的 **Context Recall** 与 **Faithfulness** 如何定义和打分？你如何基于分数反向优化检索与生成链路？

---

## 🔥 模块三：精选高频攻击题（源自常态化八股与架构补充）
> *根据另一份面试宝典提取，重点补充特定专有名词和边缘攻防场景。*

1. **分布式与组件底盘**
   * **AQS进阶**：你提到 ReentrantLock，能详细讲讲 AQS 底层那个队列具体是什么样子的吗（它的 CLH 变体队列设计）？
   * **CAP权衡**：CAP 理论下，购票场景如何在 AP 与 CP 之间权衡？“最终一致性”具体一致的对象是什么？
   * **布隆特性的绕过**：既然原生的布隆过滤器不支持动态删除，如果你不想深夜停机全量重构，有没有增加“逻辑布隆位（标记位）”或者附加特定缓存逻辑的规避重构方法？

2. **核心网关细节追击**
   * **Lua脚本崩溃**：如果 Lua 扣减脚本执行异常，Redis 是否会出现“执行到一半”的中间状态？为什么？
   * **Sentinel 算法底层**：你用的 Sentinel 说是滑动窗口限流，能讲一下 Sentinel 底层 `LeapArray` 核心数据结构是如何实现滑动窗口的时间切片流转的吗？比固定窗口好处在哪？

3. **智能 Agent 深度补充**
   * **意图与编排设计**：为什么你们 AI 系统要设计成 `Orchestrator-Worker`（路由员与执行员分离）编排架构？把意图识别 Agent 和具体执行购票任务的 Agent 分开有什么优势？
   * **流式长连接瓶颈**：使用了 WebFlux 实现流式推送（SSE）。如果是用传统的 MVC 来做 SSE，会占用死 Servlet 的工作线程从而拖垮系统吗？WebFlux 在线程调配上具体做了什么改进？
   * **语义缓存极值**：你在做 Redis 语义向量缓存（Semantic Cache）时，相似度阈值为什么定在 0.95？如果定在一个更低的数值（比如 0.8），会在你们的抢票/退改签系统中发生什么样的严重事故？

---

## 🧠 模块四：字节跳动中国交易与广告后端定向高频题
> *基于公开面经的高频方向整理，明显集中在 MySQL/Redis/MQ/并发/RPC/网络/算法，再叠加广告投放与交易一致性场景。*

### 1. 字节高频基础追问（偏一面/二面）
1. **MySQL 基础组合拳**：MySQL 一条 SQL 从客户端发出到返回结果，完整过程是什么？经过了解析、优化器、执行器、存储引擎的哪些阶段？
2. **索引和 MVCC**：B+ 树为什么适合数据库索引？聚簇索引和二级索引的回表过程是什么？MVCC 依赖哪些隐藏字段和日志？
3. **事务与锁**：可重复读为什么能解决不可重复读？幻读在 InnoDB 下是如何通过 Next-Key Lock 处理的？乐观锁、悲观锁和 MVCC 的边界分别是什么？
4. **Redis 高频**：Redis 为什么快？单线程为什么还能高性能？网络 IO 模型、内存访问模式和数据结构分别贡献了什么？
5. **缓存设计**：缓存穿透、击穿、雪崩分别怎么定义？每种在你项目里真实对应的风险点是什么？如果热点 key 过期瞬间来了 10 万请求，你的第一道和第二道防线分别是什么？
6. **Redis 数据结构落地**：如果让你用 Redis 实现 LRU、排行榜、延迟队列、去重集合，各会选什么数据结构？为什么？
7. **MQ 基础**：RocketMQ 的消息持久化、刷盘、主从复制、消费重试和死信队列怎么工作的？如果消息重复消费，你从业务层怎么兜住？
8. **RPC / 网络**：RPC 和 HTTP 调用的本质区别是什么？如果内部服务之间调用量很大，为什么大家不直接全用 REST？序列化、连接复用和超时控制怎么影响性能？
9. **网络常考**：三次握手为什么不是两次？TIME_WAIT 为什么存在？如果线上出现大量 TIME_WAIT，你会先看客户端还是服务端？
10. **并发底层**：AQS、CountDownLatch、Semaphore、ThreadPoolExecutor 的工作机制分别说一下。线程池的核心线程数、队列长度、拒绝策略你是怎么定的？
11. **JVM 常考**：CMS 和 G1 的差异是什么？线上 Full GC 频繁时你怎么排查？对象为什么会进入老年代？
12. **SQL 手撕高频**：给两张表，求每个广告主消耗 TopN、求最近 7 天转化率、求重复订单去重结果，你会怎么写 SQL？窗口函数和 group by 的使用边界是什么？
13. **算法保底题**：岛屿数量、编辑距离、表达式求值、二叉树层序遍历、滑动窗口、TopK、LRU Cache、合并区间，这几类题你的默认解法和复杂度是什么？

### 2. 交易后端定向题（资金、订单、对账）
1. **交易一致性**：交易系统和普通业务系统的最大区别是什么？为什么“订单成功但资金没扣”与“资金扣了但订单没成功”是两类不同级别事故？
2. **幂等设计**：下单、支付、退款、回调通知四个链路分别怎么做幂等？幂等键选什么？只靠数据库唯一索引够不够？
3. **分布式事务取舍**：支付/退款场景为什么有时用 TCC，有时用本地消息表或 MQ 最终一致？你怎么判断一个动作必须强一致，还是允许最终一致？
4. **对账设计**：交易系统为什么一定要有日终对账或准实时对账？订单侧、账户侧、支付侧、营销补贴侧的口径如何统一？
5. **补偿机制**：如果退款已经写库成功，但下游库存/额度/优惠券回补失败，该怎么保证最终收敛？补偿任务重放时如何避免二次回补？
6. **回调乱序**：第三方支付成功回调、用户主动查询、超时关单三个事件乱序到来时，订单状态机会怎么设计？
7. **资损排查**：如果线上某用户反馈“我被扣了两次钱”，你第一时间要拉哪些日志、查哪些表、核哪些字段？
8. **审计与可追溯性**：交易链路为什么强调 traceId、操作日志、状态流转日志、资金流水号？如果没有这些会怎样？

### 3. 广告后端定向题（投放、预算、流量分配）
1. **广告投放主链路**：一次广告请求从流量进入、召回、粗排、精排、预算校验、频控、去重、计费、回传，大致经历哪些模块？
2. **广告系统和普通推荐系统的区别**：为什么广告系统除了相关性，还要同时考虑预算、出价、eCPM、频次控制、广告主 ROI？
3. **预算一致性**：广告预算扣减为什么和库存扣减很像？如果多个机房同时扣同一广告主预算，怎么避免超扣？
4. **Pacing / 平滑投放**：什么是 pacing？为什么广告预算不能一小时就跑光？如果投放过快或过慢，系统该怎么调节？
5. **频控与去重**：同一用户同一广告短时间内不能一直看到，你会把频控做到 Redis、DB 还是本地缓存？Key 维度按用户、设备、广告还是 campaign？
6. **转化归因**：点击、曝光、转化三类事件乱序或丢失时，CTR/CVR 统计会受什么影响？怎么做去重和迟到数据修正？
7. **冷热数据分层**：广告画像、物料、预算、实时消耗、离线报表这几类数据为什么不该放在一种存储里？
8. **压测指标**：广告系统更看重 QPS、P99、可用性还是正确计费？为什么广告场景对延迟抖动特别敏感？
9. **限流与降级**：如果下游粗排服务抖动，你会优先降召回量、降特征、走缓存、还是熔断某些广告计划？为什么？
10. **A/B 实验**：广告排序策略改动后，为什么不能只看 CTR？还要看哪些业务指标，比如消耗、ROI、转化、广告主留存？

### 4. 面试官很爱顺着你项目继续拷打的问题
1. **从 12306 到广告/交易迁移**：你的项目是票务，不是广告，你觉得哪些能力是可迁移的？哪些地方必须承认场景差异？
2. **可迁移能力拆解**：库存扣减、热点防御、异步一致性、对账、幂等、流式链路、限流熔断，这些分别怎么映射到广告预算、订单、支付、回调、报表系统？
3. **承认边界**：如果面试官问“你的票务库存和广告预算完全一样吗”，你该怎么回答才能既不硬凹，又体现抽象能力？

---

## 🛠️ 模块五：围绕你当前 12306 项目的新增深挖题（基于这次真实修复）
> *这一组题非常值钱，因为它们不是泛泛八股，而是你刚刚项目里真实踩过的坑。面试官一旦听你讲到这里，可信度会明显提升。*

### 1. 对账口径与库存语义
1. **口径拷问**：`t_train.stock` 在你项目里到底表示“初始库存”还是“当前剩余库存”？为什么这个定义会直接决定对账公式怎么写？
2. **状态语义**：`CREATED / PROCESSING / PENDING / SUCCESS / FAILED / CANCELLED` 分别意味着什么？哪些状态已经扣了 Redis，哪些状态已经扣了 MySQL？
3. **公式纠错**：为什么原先 `expectedRedisStock = dbStock - pendingCount - processingCount` 是错的？错在“重复扣减”还是“状态理解错误”？
4. **正确公式**：你现在为什么改成 `expectedRedisStock = dbStock - createdCount - processingCount - refundPendingCompensateCount`？每一项背后的业务含义是什么？
5. **不可能状态识别**：什么叫 `expectedRedisStock < 0` 的 impossible state？这说明是代码 bug，还是历史数据已经自相矛盾？

### 2. refund_redis_compensated 字段深挖
1. **字段定位**：`refund_redis_compensated` 为什么不单独建一张补偿表，而是先放在 `t_order` 里？这样设计的优点和缺点是什么？
2. **状态解释**：为什么 `status = CANCELLED` 还不够，还要有 `refund_redis_compensated = 0/1`？它描述的是主业务状态，还是异步补偿进度？
3. **幂等用途**：如果 RocketMQ 同一条退款消息重复投递，`refund_redis_compensated` 如何避免 Redis 被重复回补？
4. **脏数据来源**：如果 `status = CANCELLED, refund_redis_compensated = 0` 长期堆积，意味着哪一步没闭环？

### 3. 事务提交前发 MQ 的竞态问题
1. **时序复盘**：为什么“数据库事务提交前就发补偿消息”会导致消费者看到旧状态？请你按时间线讲一遍生产者线程和消费者线程的交错过程。
2. **现象和证据**：你是怎么定位到这个问题的？日志里出现了什么关键字，数据库里又出现了什么标志性脏数据？
3. **修复方式**：为什么改成 `afterCommit` 再发 MQ 就能解决？这算业务变更，还是实现时序修正？
4. **更进一步**：如果 `afterCommit` 发 MQ 自己失败了怎么办？你会不会进一步考虑本地消息表？

### 4. 补偿逻辑抽服务的原因
1. **为什么要抽 `RefundCompensationService`**：不抽的话，`TicketTools`、MQ Consumer、后台修复接口各写一份有什么问题？
2. **统一语义**：你如何保证“线上正常消费”和“人工重放修复”走的是同一套补偿逻辑，而不是两套各算各的？
3. **代码结构收益**：把补偿逻辑集中后，对幂等、排障、重试、测试分别带来了什么好处？

### 5. 为什么 replay 之后还需要 rebuild
1. **重放不一定等于恢复**：为什么把所有 `refund_redis_compensated = 0` 的单子重放一遍，Redis 还是可能和预期差 1？
2. **历史脏数据与标记不可信**：如果历史上某条单子已经把 Redis 补过了，但标记还停留在 `0`，重放会造成什么问题？
3. **rebuild 的正当性**：为什么这时要按“当前业务事实状态”重建 Redis，而不是继续盲目补消息？
4. **修复边界**：什么情况下允许用 `stock rebuild`，什么情况下必须先停下来查业务事实，不能直接重建？

### 6. 如果面试官继续深挖，你可以被问到的追问题
1. **如果把这套逻辑搬到支付系统**：`refund_redis_compensated` 这种字段是否还能沿用？什么时候应该升级成独立的补偿任务表或流水表？
2. **如果取消单很多**：`replay` 一次扫 1000 条有没有问题？如何分页、限速、避免把 Redis 打爆？
3. **如果多实例同时 replay**：如何避免两个运维操作并发重放同一批订单？要不要加分布式锁？
4. **如果 Redis 挂了**：你的 replay 接口是应该整体失败、部分成功、还是记录失败明细继续向后处理？
5. **如果未来要做标准化审计平台**：你会把哪些字段沉淀成统一的审计模型？比如 traceId、orderSn、stockDelta、before/after、source、operator。
