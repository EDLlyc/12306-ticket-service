# 🚀 12306 智能购票系统 - RAG 专项面试宝典

结合你项目中真实的 RAG 实现逻辑（包括最新加入的 Query优化、混合检索、父子文本块等高级特性），我为你整理了这份专属的 RAG 面试高分对答宝典。

---

## 核心亮点 1: 提问重写（Query Refiner）—— 解决“口语化搜索不准”
*   **面试官发问**：如果用户提问含糊不清、非常口语化（例如：“这破车咋退票？”），导致直接去向量数据库里查不到精确规章，你的系统是怎么解决的？
*   **🌟 你的高分回答**：
    1.  **痛点**：普通用户的提问往往缺乏领域内的高频专业词汇，直接用这段口语做 Embedding 检索，召回质量（Recall）非常差。
    2.  **我的方案 (提问优化器 Query Refiner)**：我在 RAG 链路进入检索层之前，**植入了一个独立的大模型节点**。我专门为它配置了 SystemPrompt（人设：铁路系统客服问题优化专家）。
    3.  **效果**：系统会先将“这破车咋退票？”**重写（Rewrite）**为意图明确、标准化的“请问高铁列车具体的退票规定是什么？”。最后，拿着这句“优化后”的提问去向量库进行匹配。经过这一步预处理，精准命中率大幅提升！

## 核心亮点 2: 混合检索（Hybrid Search）—— 解决“专有名词失焦”
*   **面试官发问**：向量检索（Dense Retrieval）有什么缺点？你的项目里是怎么保证检索准确率的？
*   **🌟 你的高分回答**：
    1.  **向量的缺点**：大模型 Embedding 擅长捕捉“语义上的相近”，但在应对**专有名词、精确的车次号（如G1234）、特定的术语**时，它的精确度其实不如传统的关键词匹配。
    2.  **我的方案 (混合检索)**：我在架构中彻底废弃了单一的向量搜索，而是引入了**双通道检索**。
        *   **通道一 (Dense，懂语义)**：基于大模型 Embedding + **Milvus** 向量数据库做语义搜索。
        *   **通道二 (Sparse，懂字面)**：基于 **BM25 算法** 的文本确切匹配引擎。
    3.  **融合机制**：为了让两边结果结合，我实现了 **RRF (Reciprocal Rank Fusion)** 重排算法，从多个维度给文档片段重新打分。这种机制在工业界非常成熟，既保证了懂用户的“弦外之音”，又绝不遗漏具体的“关键词”。

## 核心亮点 3: Parent-Child 分块策略 —— 解决“断章取义与答非所问”
*   **面试官发问**：讲讲你在知识库构建时是怎么做 Document Chunking (文本切片) 的？切大了和切小了有什么影响？
*   **🌟 你的高分回答**：
    1.  **权衡难题**：切得太大（比如 2000字），虽然包含了完整的上下文，但在向量库中会包含太多杂乱的语义，导致检索打分不准；切得太小（比如 100字），检索打分极度精准，但交由大模型生成回答时，就会因为缺乏前因后果而“断章取义”。
    2.  **我的方案 (Parent-Child 策略)**：
        *   我做了**双层切块**。底层把知识以 400 字符切为 `Child` 块，并在它身上**绑定一个 `parent_id`**，`Child` 被向量化后存入 Milvus 中**仅作为检索诱饵**。
        *   它的父节点 `Parent` 块（保留 1500 字符的完整上下文）只存在 KV 存储（或元数据 metadata）中。
    3.  **检索过程**：用户提问命中了一个很小的 `Child` 后，系统会自动根据 `parent_id` 顺藤摸瓜，把**那个又大又完整的 `Parent` 文本块**捞出来，这才是喂给大模型做参考的最终 Context。该策略完美平衡了“高检索精准度”与“高生成准确性”。

## 核心亮点 4: Agent 工具调用 (Function Calling) —— 走向动态 RAG
*   **面试官发问**：你的 RAG 只能用来“回答死知识”（规章制度）吗？如果用户问“今天到北京的票还有没有”，系统怎么处理？
*   **🌟 你的高分回答**：
    1.  **通用 RAG 局限**：普通 RAG 依托于提前向量化的知识库，**没有处理实时动态数据的能力**。
    2.  **我的方案 (Agent + Tool)**：我在底层使用了 LangChain4j 的 `AiServices`，给大模型系统绑定了查票工具方法（如 `queryTrain()`）并打上了 `@Tool` 注解。
    3.  **大模型推理**：大模型能够感知到这些本地 Java 工具。当用户询问车次动态信息时，大模型会暂停生成，**主动触发 Function Calling**，回调我的 Java 方法实时从 DB/Redis 中读取最新库存，大模型拿到数据后再组织语言生成人性化回复。这让系统完成了从“传统客服”到“智能助理(Agent)”的质变。

## 核心亮点 5: 流式输出与多轮会话 (SSE & Memory)
*   **面试官发问**：大模型生成速度那么慢，你在前端怎么保证用户体验的？有上下文记忆吗？
*   **🌟 你的高分回答**：
    1.  **性能体验**：如果等待大模型完全生成完毕再把 Result 包装返回给用户，可能会长达十秒，严重影响体验。我使用了 **SSE (Server-Sent Events) 单向长连接技术**。调用大模型的 Stream 接口，每生成一个 Token 就通过 `SseEmitter` 往前端吐出一个字符，实现“打字机效果”，首字节响应时间降低到了百毫秒级别。
    2.  **会话级记忆**：我不仅对接了单轮问答，还引入了 `ChatMemoryProvider`。系统利用每次请求带来的 `SessionId` 维护一个滑动窗口（如只保留最近的 10 次对话），这样大模型就能处理“那另一趟车呢？”这种带指代的追问。

## 核心亮点 6: 百万级并发防御 —— 布隆过滤器 (Bloom Filter) 秒杀穿透攻击
*   **面试官发问**：如果黑客用脚本生成几百万个不存在的随机假车次号疯狂查询，你怎么防御？
*   **🌟 你的高分回答**：
    1.  **现有方案漏洞**：最初系统使用“缓存空对象（Cache Null Value）”策略，查不到的车次存为 `EMPTY`。但是在海量随机假车次攻击下，会导致 Redis 里被强塞毫无意义的空 Key，瞬间撑爆内存发生 OOM。
    2.  **我的升级方案 (布隆过滤器)**：我引入了 Redisson 的布隆过滤器（Bloom Filter）。在系统预热（Init）阶段，我会将数据库中所有「合法的车次号」全部加载进内存布隆过滤器。
    3.  **完美防御链路**：用户请求打过来时，**绝对不会先查 Redis，而是第一步先判别布隆过滤器**。如果它指出该车次不存在，直接抛出异常拦截。这样就从物理源头上把海量的恶意穿透请求精准秒杀在最外层，完美保护了核心缓存和 DB。

## 核心亮点 7: 延迟双删的局限性思考 —— 余票真正的大招
*   **面试官发问**：讲讲怎么保证 Redis 缓存和 MySQL 测试双写一致性的？抢票用了延迟双删吗？
*   **🌟 你的高分回答 (反套路压制)**：
    1.  **场景大分流**：在这个项目中，我切忌盲目套用网上的八股文延迟双删，而是按业务场景进行了严格区分。
    2.  **低频写场景（适合延迟双删）**：对于车次的时刻表变更、站点发车信息这种极少被修改的基础数据，我使用了「更新 DB -> 删除 Redis -> 延迟 500ms 后再异步删一次 Redis」经典策略，低成本消灭了并发下的脏数据残留。
    3.  **极高频写场景（坚决不用！）**：对于**项目真正的大流量核心——余票库存**，我**坚决不使用**延迟双删。因为抢票峰值流量极大，频繁删除缓存会导致查询请求瞬间击穿 Redis 进而全打入 DB，这等同于自杀。在真正的高并发余票扣减场景，我的方案是**利用 Redis Lua 脚本原子性预扣库存（拦住超卖请求），随后立即给 RocketMQ 发送事务消息，由后台异步消费者从容不迫地执行 MySQL 最终落库操作**。这种以“最终一致性”取代“强一致性”的设计，才是贴合业务的高阶架构思维。

## 核心亮点 8: 决战春运极热点 Key —— 库存分片 (Segmented Stock)
*   **面试官发问**：春运除夕夜，全国几百万人同时抢一趟车（比如网红列车 `G89`），Redis 扛得住吗？
*   **🌟 你的高分回答**：
    1.  **单点瓶颈**：极端火爆的车次会导致承载它这行数据的那个 Redis 主节点 CPU 瞬间飙升到 100%（即严重的热点 Key 倾斜），引发集群雪崩。
    2.  **我的方案 (分段库存)**：我参考了 Java 源码中 `ConcurrentHashMap` 的分段锁设计思想，把 `G89` 的 1000 张余票，均匀切分并打散到了 10 个不同的 Redis Key 里（例如：`train:stock:G89:0` 到 `train:stock:G89:9`，每个 Key 存放 100 张票的额度）。
    3.  **高并发散列路由**：当成百上千抢票请求到来时，通过 `userId % 10` 或者随机数进行 Hash 散列路由。这样就把几百万的并发洪峰平滑分摊到了 10 个不同的 Redis 节点上，打破了单机的物理处理上限，抢票吞吐量实现了十倍以上的暴力跨越。

## 核心亮点 9: 架构底线与闭环 —— 限流熔断与 MQ 事务回查
*   **面试官发问**：对于秒杀/抢单系统，除了缓存和异步，你还在架构上做了哪些系统防御？
*   **🌟 你的高分回答**：
    1.  **断臂求生 (RateLimit)**：为了防止非预期的大促洪峰直接压崩应用服务器，我在微服务网关和核心 Controller 引入了 **Sentinel / Guava RateLimiter** 进行流量控制。明确设定单节点核心接口 1000 QPS 的安全阈值，超过则快速失败返回系统繁忙，为系统大动脉兜住底线。
    2.  **事务闭环 (TransactionListener)**：大家都在用 RocketMQ 异步下单，但我不仅发送了 Half 半消息，为了数据严谨，我还专门实现了对应的 `TransactionListener` 提供本地事务执行逻辑和**状态回查机制**。这保证了哪怕由于 OOM 或断电导致系统重启，MQ Broker 也能通过定时回查，确认确认本地订单数据是否落库成功，达成订单动作与车票扣减 100% 的最终一致性闭环。

## 终极 RAG 加分项 (冲击大厂 SSP/SP 必杀技)

### 进阶亮点 1: 终极精准排序 —— 引入重排模型 (Reranker) `[✅ 核心代码已在 ZhipuReranker.java 落地]`
*   **面试官发问**：“你既然用了混合检索，找回来的比如 10 个片段，怎么决定哪 3 个送给大模型最合适？”
*   **🌟 你的高分回答**：
    1.  **痛点分析**：目前的 RRF (交叉倒数排序) 算法只是基于统计学的粗略融合，不够精细。
    2.  **我的方案 (Reranker)**：我在真正的检索引擎和 LLM 之间，加入了一个基于 Cross-Encoder 架构的重排模型（如 `bge-reranker`）。这就是经典的检索漏斗：混合检索负责“粗排召回”（扩大召回率，防止漏掉关键信息）；Reranker 负责“精排打分”（提高精确率，优中选优）。
    3.  **防低级错误**：这一粗一精的架构，使得最终喂给大模型的上下文绝对纯净，从根本上消灭了因为塞入无关上下文导致模型产生幻觉的可能。

### 进阶亮点 2: 大模型降本提效 —— 语义缓存 (Semantic Cache) `[✅ 核心代码已在 RagServiceImpl 落地]`
*   **面试官发问**：“大模型生成慢且按 Token 收费。遇上春运几万人同时问‘儿童票怎么买’，怎么优化耗时和成本？”
*   **🌟 你的高分回答**：
    1.  **痛点分析**：如果每次都去走一遍“向量检索 -> 大模型生成”的完整耗时链路，不仅 API 烧钱，而且并发极低。
    2.  **我的方案 (语义缓存)**：我引入了基于 Redis + 向量计算的语义缓存层。当用户提问时，系统会先将提问向量化，去 Redis 里寻找之前有没有**语义相似度极高**（> 0.95）的历史提问（比如‘儿童票怎么买’和‘小孩怎么买票’实际上是一回事）。
    3.  **价值**：如果命中，直接把 Redis 里缓存的 LLM 历史答案瞬发给前端。首字节响应时间降低到了几毫秒，不仅绝对扛住了大并发，更省下了海量的 Token 费用。

### 进阶亮点 3: 系统的最强大脑 —— 意图路由 (Query Routing) `[✅ 核心代码已在 RagServiceImpl 落地]`
*   **面试官发问**：“如果用户输入‘你好’或者‘今天天气怎么样’，你的系统的处理流程是什么？”
*   **🌟 你的高分回答**：
    1.  **盲区痛点**：不能所有的 query 都一股脑丢进知识库查，这不仅慢，而且查出来的绝对是不相关内容导致 AI 胡言乱语。
    2.  **我的方案 (意图路由)**：我在整个 RAG 的最前端加入了一个“意图路由器 (Router)”。
    3.  **分流逻辑**：它负责把用户请求精准分流到三个通道：①“闲聊意图” -> 直接走普通 LLM 生成；②“实时动态查询”（如查余票/查晚点） -> 走 Agent Function Calling（直连 DB）；③“制度规章意图” -> 才去走 Milvus 的 RAG 检索链路。这让系统真正具备了大脑调度的能力。

### 进阶亮点 4: 数据预处理的深水区 —— 非结构化解析 `[❌ 仅提供高分话术，重度依赖多模态集群]`
*   **面试官发问**：“公司如果有几万份包含**复杂表格**和**图片**的 PDF 规章说明书，你能直接切块存向量吗？”
*   **🌟 你的高分回答**：
    1.  **粗暴切分的代价**：普通的纯文本可以按字符数做“父子块”切分，但如果把一张表格拦腰斩断，它的行列语义就全毁了。图片更是完全无法直接被 Embedding。
    2.  **我的高阶方案 (多模态 ETL)**：在数据预处理阶段，对于表格，我引入了专门的布局分析大模型，将表格像素提取转化为完整的 Markdown 表格结构；对于包含核心流程图的照片，我会先调用多模态大模型（如 GPT-4o 或 Qwen-VL），把图片里的含义提炼总结成文字描述，最后再连同原文件名一块存入向量库。记住，Garbage in, Garbage out，只有数据源足够干净结构化，RAG 才有上限。

### 进阶亮点 5: 闭环杀手锏 —— 怎么做自动化评估 (RAGAS) `[✅ 核心代码已在 ragas_eval/ 落地]`
*   **面试官发问**：“你凭什么说你的 RAG 系统比原来更好？你怎么量化测试它到底幻没幻觉？”
*   **🌟 你的高分回答**：
    1.  **人工测试的局限**：绝大多数工程师只靠自己提问几次，凭肉眼和主观感觉去判断错对，这在工程上是极度不严谨的。
    2.  **我的方案 (LLM-as-a-Judge)**：我参考了业界最成熟的 RAGAS 评估框架，采用了“让大模型当裁判”的思路。
    3.  **三大核心看板**：每次优化完架构，我的 CI/CD 流水线都会自动化跑几百个测试用例，从三个维度打分验证：**Faithfulness (忠实度)**（回答有没有胡编乱造）、**Answer Relevance (回答相关度)**（有没有切中提问要害）、**Context Precision (上下文精准度)**（检索出的文档真的有用吗）。我是用客观的数据去支撑系统架构的迭代！

## 其他基础 RAG 八股 (保底必备)
1. **什么是全量微调 (Fine-Tuning) 和 RAG 的区别？**
   * 微调成本极高、容易遗忘并且无法实现权限隔离（把高管特权也学进去了）。RAG 是给模型外挂一个极速优盘，知识随时可以增删改（重新上传 PDF 即可），且回答具备清晰的引用来源，成本低，性价比极高。
2. **Milvus 是什么？能用 Redis 替代吗？**
   * Milvus 是专为海量向量数据设计的云原生分布式向量数据库。Redis 虽然也有 RedisSearch 提供向量能力，但在海量高维度数据下，Milvus 的底层索引构建和跨机器存算分离具备绝对优势，是中大型企业更正规的技术选型。
