# 🎯 12306 工程师级高频模拟面试题库 (深挖版)

这份文档基于你的高质量简历为你量身定做了“常规必问题”和极具压迫感的“连环深挖题”。面试官在看到你的简历后，一定会对诸多进阶技术点（如：分段库存、响应式背压、Cross-Encoder、RAGAS、RocketMQ 事务）产生浓厚兴趣。

建议你对着这些问题进行录音自问自答练习。

---

## 一、 12306 高并发售票核心网关（后端架构开发）

### 模块 1: 多级缓存与防穿透/击穿体系
**🔴 常规热身题：**
1. 你的 Caffeine + Redis 二级缓存架构具体是怎么配合的？如果 Caffeine 有数据但 Redis 没有怎么办？
2. 布隆过滤器的底层原理是什么？为什么你选它做防穿透屏障？
3. 能不能手写或者口述一下应对缓存击穿的 RLock + DCL（Double Check Lock）的核心代码结构？为什么必须要 Double Check？

**🔥 地狱深挖题 (区分度高)：**
1. **布隆过滤器缺陷：** “布隆过滤器不支持删除，如果系统临时取消了某个车次（比如台风停运），或者新增了车次，你是怎么动态更新布隆过滤器的拦截规则的？” 
   *(💡提示：可以回答通过定期重建、双层滤布切换，或者结合位图/白名单机制兜底。)*
2. **多级缓存一致性：** “本地缓存 Caffeine 各个节点都是独立的。如果车次的状态在 DB 里被运营人员修改了，你怎么保证所有微服务实例内部的 Caffeine 都能及时失效，而不是读到脏数据？”
   *(💡提示：可以通过 Redis Pub/Sub 或者 RocketMQ 广播消息，实时通知所有集群节点主动 `invalidate` 自己的本地缓存。)*

### 模块 2: 流量调度与分段库存 (Segmented Stock)
**🔴 常规热身题：**
1. 为什么不用单纯的 Redis `decr` 扣减，而必须要手写 Lua 脚本？
2. 遇到热门车次（如京沪高铁），单 key 并发写入到达 Redis 瓶颈，你怎么解决的？

**🔥 地狱深挖题 (区分度高)：**
1. **分段库存的惊群效应：** “简历里提到了分段库存（Segmented Stock），你是怎么给库存分段的？如果总库存 1000 拆分成 10 个每段 100 的 Bucket。现在用户想买票，正好被路由到了 Bucket-1，但 Bucket-1 已经卖光了（库存为0），而其他 Bucket 还有票，此时你怎么处理才不会误判卖完？”
   *(💡提示：当个别 Segment 到底时，可以尝试自旋去拉取或借用其他 Bucket 的库存，甚至在最后阶段将分段合并退化为单点统一扣减。)*
2. **本地事务与缓存回补：** “你提到通过 `@Transactional` 保障多端退费与回补。如果 `@Transactional` 里面的 MySQL 操作成功了，但是方法结束前，通过网络请求回补 Redis 库存却超时了（引发回滚），此时到底成功还是失败？”
   *(💡提示：考察事务的粒度和缓存的无状态性。写数据库必须先成功，缓存即使出错也可以通过异步或定时任务补偿，不能因为缓存更新失败直接阻塞主支付事务。)*

### 模块 3: 分布式事务与最终一致性
**🔴 常规热身题：**
1. 请简述 RocketMQ 事务消息的“三阶段提交”（发送半消息、执行本地事务、本地事务回查）的全流程。
2. 在 `executeLocalTransaction` 中，如果订单落库由于主键冲突失败了，你的补偿机制是怎么起效的？

**🔥 地狱深挖题 (区分度高)：**
1. **网络超时引发的双重确认：** “如果你的本地事务执行成功了（落了 DB），在发 `COMMIT` 给 MQ 时网络断了。MQ 几秒后触发 `checkLocalTransaction`。在回查过程中，又是由于数据库卡顿，查库也超时返回了 `UNKNOWN`。这时候会发生什么？”
   *(💡提示：RocketMQ 会按照递退间隔（最大15次）持续发起重试回查，直到拿到最终明确的 COMMIT 或 ROLLBACK。如果15次全败，消息将被打入死信队列或丢弃告警。)*
2. **MQ 顺序性与并发控制：** “如果你在这个链路里要处理的是【退改签】这种有序状态跳变操作，如何保证下单（Create）、付款（Pay）、退款（Refund）消息能按顺序被单个消费者消费，且不丢失最终状态？”
   *(💡提示：MessageQueueSelector 按 HashKey (如订单ID) 路由到指定的队列中。)*

---

## 二、 12306 智能数字员工 (Reactive Multi-Agent)

### 模块 1: 工业级多智能体角色编排
**🔴 常规热身题：**
1. 为什么不把“查票库”和“规章库”的能力都写在同一个大 Prompt 里交给一个 LLM 去做，非要拆开成 Orchestrator-Worker 架构多智能体？
2. “实体桥接 (Entity Bridging)”和“Session分发隔离”是怎么落地的？这解决了 LangChain4j 的什么原生痛点？

**🔥 地狱深挖题 (区分度高)：**
1. **意图路由飘逸：** “如果用户提问既有查询又有操作，比如：‘帮我退掉G123的票，另外告诉我儿童免票身高是多少’。你的 Query Router 遇到这种复杂、双挂钩的指令，是怎么进行意图拆分和路由分发的？”
   *(💡提示：可以解释为将主辅意图分离，或者先走操作流卡住状态，再走纯问答并发流最后合并结果。如果没有做这么深，可以如实回答“目前基于权重策略选取最高意图，后续可引入抽象语法树重构提问”。)*

### 模块 2: 响应式流式引擎与 WebFlux
**🔴 常规热身题：**
1. 你使用的 Spring WebFlux 和传统的 Spring MVC（Tomcat 线程池）有什么本质的区别？
2. 什么是背压 (Backpressure)？你在简历中写的 `Sinks.Many` 怎么控制背压？

**🔥 地狱深挖题 (区分度高)：**
1. **WebFlux 的坑：** “响应式编程 (Project Reactor) 最怕的就是阻塞，如果你在 WebFlux 的流处理中调用了一个不支持异步的 JDBC 库（如老版本 MyBatis）或老系统的 HTTP 接口，会导致整个 Netty 线程池假死。你在开发时是怎么隔离或解决这些同步 IO 阻塞的？”
   *(💡提示：使用 `subscribeOn(Schedulers.boundedElastic())` 将同步阻塞任务转移到弹性的阻塞专用线程池里，避免饿死底层的 EventLoop 线程。)*

### 模块 3: RAG 混合检索与精排（核心算法含金量区）
**🔴 常规热身题：**
1. 讲讲什么是 Parent-Child 检索策略？为什么要有大块（Parent）和小块（Child）两个级别？
2. 稠密向量（Dense Milvus）和稀疏检索（BM25）各自的优缺点是什么？结合使用为什么效果会更好？

**🔥 地狱深挖题 (区分度高)：**
1. **RRF 算法：** “由于 BM25 的得分可能是 200 分，而余弦相似度最大是 1。你用 RRF (倒数秩融合算法) 融合时，具体的数学逻辑是怎样的？怎么平衡这两者的权重分歧？”
2. **Cross-Encoder：** “为什么还要加一层 Cross-Encoder Reranker 型模型进行重排？比起单纯拿第一轮召回前五名的结果直接丢给生成大模型，Reranker 在注意力计算上到底牛在哪里？”
   *(💡提示：Bi-Encoder（常规向量库提取的底层逻辑）是由于性能考虑，将 query 和 doc 先期各自单独提取向量再点乘计算，忽略了二者内部词汇级的深刻语义依赖；而 Cross-Encoder 是将 `[CLS] Query [SEP] Doc` 合并喂给模型直接做注意力全连接，速度慢但准确度极高，适合放在最后一级做精准漏斗重排。)*

### 模块 4: 客观质量评估 RAGAS
**🔴 常规热身题：**
1. 简单介绍一下什么是 RAGAS？你为什么要在微服务之外单独用 Python 起这样一个系统？
2. 在对你的系统进行 Faithfulness（忠实度）和 Context Precision 评分后，你得出了哪些结论指导你后续的代码改进？

**🔥 地狱深挖题 (区分度高)：**
1. **LLM-as-a-Judge 的局限性：** “使用 LLM 作为裁判来打分，它自己也会出现误判甚至幻觉，可能给错误的答案打高分。你如何验证并处理 RAGAS 框架本身的不稳定性（评估系统本身的偏差度）？”
   *(💡提示：提供“少量极高质量的人工标定黄金数据集 (Golden Dataset)”来抽检验证 LLM Judge 的准确性。)*
2. **记忆机制压缩算法：** “你的长短期滑动记忆机制里提到了‘长期摘要压缩’，如果用户前面的历史对话包含了极度碎片化的乘车人信息（比如一会说张三去，一会又改口去李四），经过 LLM 压缩成摘要的时候信息失真了或者丢失了核心主键，导致后面工具无法调用，怎么恢复呢？”
   *(💡提示：长短期结合策略。近期高频核心事实可以用 Redis Entity Cache (KV键值对形式) 精准备份，而不是依赖纯自然语言概括；自然语言摘要主要只用于留存“对话氛围与弱事实”。)*

---

## 三、 补充常考经典八股 (大厂直通车)

结合 12306 抢票项目的高并发背景，面试官极有可能在深挖完项目后，画风一转，直接抛出底层“八股”来考察你的基础扎实程度。以下是与项目强相关的必背高频八股：

### 模块 1: MySQL 底层与高并发优化
1. **索引原理：** 为什么 MySQL InnoDB 选择 B+ 树而不是 B 树或红黑树？
2. **事务隔离：** 讲讲 MySQL 的 MVCC（多版本并发控制）机制是怎么解决不可重复读的？能完全解决幻读吗？
3. **锁机制：** 你的车次库存扣减除了用 Redis，如果落到底层 DB 去扣，怎么防止超卖？(行级锁 / 乐观锁版本号 / Gap Lock 间隙锁防幻读)。
4. **慢 SQL 排查：** 如果线上 `t_order` 表慢查询告警，你会怎么排查？(EXPLAIN 分析、覆盖索引、最左前缀法则、深度分页优化)。

### 模块 2: Redis 高可用与性能底盘
1. **持久化灾备：** RDB 和 AOF 有什么区别？如果你的 Redis 宕机重启，怎么恢复能使数据丢失最少且恢复最快？（混合持久化）。
2. **内存淘汰：** 12306 春运期间海量查询缓存，如果 Redis 内存打满了，你配置的是什么淘汰策略？(LRU / LFU)，聊聊 Redis 近似 LRU 的实现原理。
3. **高可用集群：** Redis 哨兵模式 (Sentinel) 是怎么做到自动故障转移的？脑裂问题怎么产生的，怎么配置从而尽量避开脑裂？
4. **分布式锁底座：** 你说你用了 Redisson，它的 WatchDog 看门狗机制是怎么续期的？如果业务还没执行完机器直接断电了，会发生死锁吗？

### 模块 3: JUC 多线程与并发原理
1. **AQS 底层：** `ReentrantLock` 和底层 AQS (AbstractQueuedSynchronizer) 的运转机制是什么？公平锁和非公平锁在实现上有什么区别？
2. **锁升级：** 详细说一下 JDK1.6 之后 `synchronized` 关键字的锁升级过程（无锁 -> 偏向锁 -> 轻量级自旋锁 -> 重量级锁）。
3. **ThreadLocal：** 你们用来存当前登录用户 TraceId 的 ThreadLocal，如果不调用 `remove()` 为什么会导致内存泄漏（OOM）？强引用与弱引用的原理解释。
4. **线程池参数计算：** 如果是像 AI LLM 这种 IO 密集型的任务，线程池的核心线程数该怎么评估设置？如果是 CPU 计算密集型任务呢？

### 模块 4: JVM 调优与线上排障
1. **类加载机制：** 什么是双亲委派模型？Tomcat 为什么要破坏双亲委派模型？
2. **垃圾回收算法：** CMS 收集器和 G1 收集器的核心区别在哪？为什么大内存下现在推荐用 G1 或者是 ZGC？
3. **线上 OOM 救火定位：** 如果 12306 服务突然 CPU 飙高到了 100%，或者频繁发生 Full GC，你拿到终端权限后，第一手排查命令应该敲什么？(top, jps, jstack, jmap -histo/dump 分析)。


