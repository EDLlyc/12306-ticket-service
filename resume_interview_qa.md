# 12306 工程师面试攻防宝典（基于简历逐句拆解）

这份文档基于你简历上的每一句话，为你预测了面试官可能发问的“连环炮”。在面试前，请确保你能用自己的话将以下问题的原理讲清楚。

## 🛡️ 模块一：12306 高并发售票核心网关（后端面经预测）

### 1. 服务限流与工程高可用规范 (Sentinel + 网关 + 异步池)
* **Q1:** 为什么选用 **Sentinel** 而不是传统的 Nginx 限流或者 Guava 的 `RateLimiter`（令牌桶）？
* **Q2:** Sentinel 的**滑动窗口限流**底层是怎么实现的？你可以画一下它的时间片数组（LeapArray）流转原理吗？
* **Q3:** 你的项目中，核心订票接口的**限流阈值**是怎么确定的？如何在全链路压测中支撑**万级 QPS**？
* **Q4: [熔断与异常兜底]** 遇到极端突发流量触发熔断后，你的系统具体是如何兜底的？为什么你强调必须要“全局统一返回值与异常捕获网关”？
* **Q5: [并发防宕机]** 为什么你要刻意手写全局 Daemon `ThreadPoolTaskExecutor` 替代裸线程或自带的 `Executors.newFixedThreadPool`？具体做了哪三层（容量、背压、停机）防御？

### 2. 多级缓存防穿透 & DCL 防击穿 (Caffeine + Redis + Redisson)
* **Q1: [缓存协作]** 为什么要构建 Caffeine + Redis 二级缓存体系？本地缓存和 Redis 之间是怎么同步失效的？如何避免数据不一致？
* **Q2: [布隆过滤器]** 布隆过滤器的底层结构是什么（多次 Hash + 位图）？为什么它能完美防止**缓存穿透**？
* **Q3: [陷阱题 - 必发散]** 如果 12306 临时新增或取消了一列真实车次，你的布隆过滤器**支持动态删除吗**？如果不支持，你是怎么做数据更新隔离或兜底的？
* **Q4: [防击穿防雪崩]** 什么是缓存击穿？为什么要用 Redisson 锁而不用普通的 Redis `setnx`？（关键术语：看门狗 watch dog 自动续期）。
* **Q5: [DCL 核心题]** 为什么你的代码里要写 DCL（双重检查锁）查两次缓存？如果在拿到 Redisson 锁之后不再次检查缓存而是直接查 DB，高并发冷启动时会发生什么致命灾难？

### 3. Redis Lua 原子扣减 & 分段库存 (Segmented Stock)
* **Q1:** 为什么扣减库存必须写 **Lua 脚本**？为什么不能在 Java 里先查出来再扣减，或者用 Redis 本身的事务（`MULTI`/`EXEC`）？
* **Q2:** Redis 执行 Lua 脚本能绝对保证原子性吗？如果 Lua 执行到一半报错了，前面执行的数据能自动回滚吗？
* **Q3: [压测验证]** 提交代码前，你是怎么写**高密度并发单测 (CountDownLatch)** 去硬核验证 Lua 扣减绝对零超卖的？
* **Q4: [亮点发散]** 你的“**分段库存 (Segmented Stock)**”具体是怎么设计打散热点 Key 压力的？
* **Q5: [退费强一致性]** 你简历里提到“保障退费查写入库与 MySQL/Redis 双端库存回补的原子性”，具体是怎么做的？为什么不用 MQ 而是直接加 `@Transactional`？如果 Redis 回补失败怎么办？

### 4. 摒弃延迟双删 & RocketMQ 事务最终一致性
* **Q1:** 解释一下业界常用的 **Cache Aside (旁路缓存)** 策略？为什么大家都建议“先写 DB，再删缓存”而不是“先删缓存”？
* **Q2:** 什么是**延迟双删**？为什么你在简历上严厉宣告“摒弃”了它？让主线程休眠数百毫秒处理双删会导致系统面临什么并发大坑？
* **Q3:** RocketMQ 的**事务消息**状态流转是怎样的？（Half 消息 -> 本地执行 -> Commit/Rollback）。
* **Q4: [兜底闭环]** 你的 `TransactionListener` 本地执行逻辑是怎么串联 DB 订单唯一索引的？如果在落库时宕机，RocketMQ 迟迟没收到回调，底层的回查机制会怎么阻断并触发 Redis 库存自动回补？

---

## 🧠 模块二：12306 智能数字员工 (AI Agent / RAG 面经预测)

### 1. 多工具 ReAct 智能体架构集成
* **Q1:** 你的 Agent 只能查票吗？和普通的只做纯文本问答的 RAG 有什么本质区别？（重点突出 **读写混合调度** 能力）。
* **Q2:** 大语言模型到底是怎么调用 12306 外部“查询/退款/状态更新”三大 API 接口的？解释一下 Function Calling 的请求与拦截机制。
* **Q3:** 如果你的 Java 查票外挂接口非常慢，大模型等待会卡死线程吗？你是怎么利用 SSE 流式预响应来化解高并发雪崩的？

### 2. Guardrails 安全护栏 & 意图路由
* **Q1: [AI 安全痛点]** 什么是 **Prompt Injection (指令注入与越狱攻击)**？你是如何构建**三层纵深防御体系 (Guardrails + 结构隔离 + Role 隔离)** 来物理阻断恶意脚本的？
* **Q2:** 你的**意图路由器 (Router)** 是如何在毫秒内判定用户是在“查票”还是在“闲聊”的？为什么说这节省了极大的 Token 计算成本与数据库压力？
* **Q3:** 引入前置的 Query Refiner 必定会导致整体对话的响应链路被拉长，你是怎么权衡系统耗时和 RAG 后续检索精度的？

### 3. 混合检索 & Cross-Encoder & 白盒单测背书
* **Q1: [数据切割]** 什么是 **Parent-Child（父子块）** 嵌套切分？为什么不能拿着原始长文本直接按绝对字数做一刀切的 Chunk？
* **Q2: [混合寻回]** Milvus 的 Dense 向量检索和原生 BM25 稀疏索引有什么本质区别？为什么必须要两者叠加？
* **Q3:** 两路检索返回的分数标准完全没有可比性，你怎么做合并排位的？（深入解释一下 **RRF 倒数等级融合**的数学叠加公式）。
* **Q4: [容灾降级]** 既然你的 **Reranker (Cross-Encoder 精排模型)** 打分那么精准，如果外部 API 突然挂了或超时，系统会立刻处于瘫痪不可用状态吗？（引出捕获异与粗排直接兜底下发的防御性编程策略）。
* **Q5: [极佳研发素养]** 为什么你在简历中刻意强调核心算法“提供零外部依赖的纯白盒单测背书”？这比业界通用的黑盒集成联调高级在哪里？

### 4. 双层记忆架构层 & Semantic Cache (语义缓存)
* **Q1: [长文本失忆痛点]** 纯粹的 `MessageWindow` 如果超出了 10 轮必定丢弃最早的数据导致大模型“失忆”。你的**双层记忆架构网络**是如何靠 LLM 压缩摘要和 Redis 24小时 TTL 来突破 Token 限制难题的？
* **Q2: [性能核心]** 你的 Semantic Cache 为什么要迁移到 **Redis 分布式架构**？命中判定标准是什么机制？为什么判定相似度阈值要定得那么高（0.95）？
* **Q3: [灵魂拷问]** 既然做了相似缓存，如果第二天后台管理员修改了 12306 的退费赔偿规则，你怎么确保其它集群节点绝对不会取到昨天 Redis 里存入的旧“语义近似答案”脏数据？
* **Q4:** SSE 单向长连接与 WebSockets 有何区别？为什么大模型字字吐出的响应，用 SSE 是最优雅低开销的方案？

### 5. 自动化量化评估 RAGAS (LLM-as-a-Judge)
* **Q1:** 人工写用例肉眼看效果一直是个糟糕的方案。你引入的 **LLM-as-a-Judge** 架构颠覆了什么痛点？
* **Q2:** 黄金数据集（Golden Dataset）大概是什么规模和维度的？
* **Q3:** RAGAS 量化打分中的 **Faithfulness (忠实度)** 和 **Relevance (相关度)** 这两个黄金指标分别是怎么计算出来的？它们各自防范了大模型的什么恶习？
* **Q4:** 如果 Ragas 自动化流水线评估发现你的 Context Precision 暴跌，你会怎么基于这个数据指标去改造你的 RAG 混合召回通道结构？
