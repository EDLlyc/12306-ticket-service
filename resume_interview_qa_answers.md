# 12306 面试攻防宝典 —— 绝杀版答案解析

这份答案严格贴合你的项目经历和简历（具备极强工程直觉与源码理解能力的大厂候选人）。在回答时，**核心逻辑线：永远把技术选型的背景建立在“12306 春运极高并发冲击”与“AI 准确性权衡”这两大痛点之上！**

---

## 🛡️ 模块一：12306 高并发售票核心网关

### 1. 服务限流与工程高可用规范 (Sentinel + 网关 + 异步池)
* **Q1: 为什么不用 Nginx 限流或 Guava RateLimiter？**
  * **答:** Nginx 仅支持入口层粗粒度限流，Guava `RateLimiter` 则是单机版令牌桶，均无法解决集群全局限流的痛点。Sentinel 能基于集群进行精确的方法级滑动窗口限流，结合其动态 CPU 自适应防护机制，能在秒级内熔断异常链路（如突增千级 QPS 瞬间熔断降级），彻底隔绝雪崩。
* **Q2: Sentinel 滑动窗口限流原理 (LeapArray)？**
  * **答:** 底层依赖环形数组 `LeapArray`（划分多个时间窗口格子）。请求进入时，通过 `当前时间戳 % 数组长度` 定位目标窗口，并用 `LongAdder` 进行高并发无锁原子累加。窗口过期自动重置，完美消除了传统固定窗口“跨秒临界点双倍请求穿透”的缺陷。
* **Q3: 限流阈值怎么确定的？怎么支撑万级 QPS？**
  * **答:** **绝非拍脑袋决定，而是压测产出的绝对客观数据。** 先在预发环境通过 JMeter 阶梯加压测得单机极限（如 1000 QPS），为保留安全缓冲，单机定水位在极限值的 80%（即 800 QPS）。有了极强悍的单机基准，在线上全链路压测中，系统仅需通过水平横向扩容（如数十台实例集群），加上分段库存打散热点，便能轻松支撑起简历上写的“万级并发吞吐量”。
* **Q4: [熔断与异常兜底] 为什么强调必须要“全局统一返回值与异常捕获网关”？**
  * **答:** 熔断降级或非法请求会导致代码层面抛出 `BlockException` 甚至 `RuntimeException`。如果直接把 500 堆栈错误抛给前端，不仅极度不专业还会导致页面白屏崩溃。我实现了 `@RestControllerAdvice` 异常网关拦截所有报错，包装为统一标准的 `Result` DTO（如状态码 200，业务码 10001 系统繁忙）。前端捕获该特征码后优雅展示“排队人数过多请重试”，将整个系统的高可用闭环做到了用户界面层。
* **Q5: [并发防宕机] 为什么必须手写全局 Daemon `ThreadPoolTaskExecutor` 替换裸线程或 `newFixedThreadPool`？具体做了哪三层防御？**
  * **答:** 这是血泪教训换来的。原生的 `newFixedThreadPool` 底层是一个容量为 `Integer.MAX_VALUE` 的无界队列，在 12306 这种高并发量冲击下，极易堆积海量异步任务（如 SSE 消息推流等操作）导致 OOM 彻底宕机。为此我重构为 Spring 原生管理的线程池并建立三道硬核防线：
    1. **容量防线**：将阻塞队列严格限制在明确的数值（如 100），截断无底洞缓冲，物理防范内存溢出。
    2. **背压防线**：引入 `CallerRunsPolicy` 拒绝策略，当异步线程与队列双双打满时，强令上游发起者自己同步执行，天然实现反向压制降速 (Backpressure)，给整个系统喘息空间。
    3. **停机防线**：显式调用 `setDaemon(true)` 将其标记为守护线程，确保在运维重启发布服务时，后台缓冲推送能干脆销毁，绝不导致老服务僵死挂起！

### 2. 多级缓存防穿透 & DCL 防击穿 (Caffeine + Redis + Redisson)
* **Q1: 为什么构建二级缓存？如何避免缓存与 DB 不一致？**
  * **答:** 面对百万级短时查询，纯 Redis 仍有显著的网络及序列化开销延迟（1-10ms）。利用 Caffeine 作为本地 L1 缓存，能在微秒级阻断 90% 热点读。
  * **一致性策略**: 采用 **Cache Aside** 模式。写操作先动 DB 再删 Redis。本地 Caffeine 采用**极短 TTL (如 3-5 秒)** 容忍微量延迟，最终以提单扣减为准。
* **Q2: 布隆过滤器底层原理？为何能彻底拦截缓存穿透？**
  * **答:** 底层是 BitMap + 多重 Hash。在 Redis/DB 前执行布隆探测：只要任一 Hash 位为 0，断定数据不存在并物理阻断。
  * **工程细节**: 使用 Redisson `RBloomFilter` 预热合法车次。通过 `tryInit(100000, 0.03)` 权衡内存占用与误判率。
* **Q3: 临时新增/取消车次，布隆支持动态删除吗？**
  * **答:** **标准布隆不支持删除**。本项目采用**凌晨异步全量重建**。对于误判漏过的极少量请求，配合 Redis “空值缓存”（Cache Null Object）逻辑进行二级兜底。
* **Q4: 什么是缓存击穿？为什么必须用 Redisson 看门狗锁？**
  * **答:** 指热点 Key 失效瞬间万级并发涌入 DB。Redisson 核心在于 **Watchdog (看门狗)**：只要线程未结束，后台自动为锁续期，彻底消除锁提前释放引发的“并发重载 DB”灾难。
* **Q5: [高频] 解释 DCL (双重检查锁) 的必要性？**
  * **答:** 核心是**阻断排队线程的重复无效 IO**。第一个线程拿到锁查 DB 回填，剩下线程在锁外排队。如果不做二次 `if(cache != null)` 校验，锁释放后排队线程会再次依次查库，瞬间击垮数据库。

### 3. Redis Lua 原子扣减 & 分段库存 (Segmented Stock)
* **Q1: 扣减库存逻辑为什么必须封装在 Lua 脚本里？**
  * **答:** 分布式并发下，“查询余票 -> 判断 >0 -> 执行 DECR”拆分在 Java 代码侧会造成时间差错觉导致超卖。Redis 单线程执行 Lua 脚本具备绝对排他性，确保这复合三步指令如原语般不可分割。
* **Q2: Lua 执行一半挂了会回滚吗？一致性怎么兜底？**
  * **答:** **Redis Lua 没有类似 MySQL 的 Undo Log 回滚机制**。一旦中途数据报错，已执行指令不会撤销。但我的预扣脚本极简且无外部依赖判断条件（仅单纯数值扣减），只要解析通过就不会发生一半挂掉的业务脏逻辑。
* **Q3: [亮点] 提交代码前，怎么硬核验证 Lua 绝对零超卖的？**
  * **答:** 不能只相信理论，我要看真实碰撞。我手写了基于 `CountDownLatch` 发令枪的**纯白盒高密度并发单测**，不仅摆脱了依赖，还在单毫秒内释放几百个线程疯抢一处内存状态变量。这种远超真实网关密度极限的微观碰撞测试，从源头闭环证明了逻辑架构的死磕严谨。
* **Q4: 分段库存 (Segmented Stock) 是如何打散热点 Key 的？**
  * **答:** 当全国人民秒杀“同一车次”时，纵然是 Redis 所在网卡也会被打挂。我通过水平分拆，将总库存 `1000` 均匀哈希分布到 `stock_seq_1` 至 `10` 这十个独立的缓存节点桶中。通过前端用户 ID 哈希随机路由削峰，实现了真正高可用的并发扩容。
* **Q5: [新亮点] 退费查写入库与双端库存回补的原子性是如何保障的？如果 Redis 回补失败怎么办？**
  * **答:** 这是一道关于“核心业务容灾兜底”的实战题。在 Agent 触发退费时（`cancelOrder` 工具），我使用 Spring 的 `@Transactional` 强包装了【更新订单状态】与【回补 MySQL t_train.stock】这两步核心写操作，确保关系型数据库底座的绝对一致性；**而将 Redis 的 `increment` 回补动作放在这同一个物理事务闭环内作为最终步骤。**
    即使极其倒霉，最后一步 Redis 回补时网络断了报错，依靠外层异常抛出，MySQL 数据会回滚，保持业务不发生“脏退费”。如果是 Redis 回补动作“自己没抛异常但实际没生效”，虽然此时 Redis 票数比实际少一张，但这能被系统接受（宁可少卖一张，绝不超卖）。而且系统下一次的全量/增量库存预热会自动从正确的 MySQL 库加载并覆盖纠正 Redis，实现了**“强一致优先，最终一致兜底”**的最佳容错实践。

### 4. 摒弃延迟双删 & RocketMQ 事务闭环
* **Q1: 何为 Cache Aside？为何业内的金科玉律是“先更新 DB 再删缓存”？**
  * **答:** 先删缓存会让并发进来的高频读线程取到还未更新成功的 DB 脏数据并重新强刷进 Redis，导致终身脏数据留存。先写 DB 后清缓存哪怕偶发性删除失败，结合 TTL 的兜底也不至于产生难以追溯的幽灵脏数据。
* **Q2: 什么是“延迟双删”？你为何强烈摒弃它？**
  * **答:** 原意是通过写 DB 前后各删一次外加 `sleep` 来躲避主从延迟引发的极短时差。在 12306 秒杀主线程中强行 `Thread.sleep(500)` 将毁灭单机的承载绝对吞吐上限，导致所有 HTTP 工作线程全部夯死，引发全盘血崩。
* **Q3: RocketMQ 事务消息机制的三个动作是什么？**
  * **答:** 1. 发射 Half 预备状态消息；2. JVM 执行本地核心业务事务（如生成订单）；3. JVM 根据落库成功与否通知 MQ `Commit` 释放彻底可见或 `Rollback` 丢弃。
* **Q4: `TransactionListener` 落网回查兜底逻辑如何设计以防钱货两空？**
  * **答:** 网络波动会导致步骤 3 通知失败。依靠 MQ 调度发起的 `checkLocalTransaction`。我在里面根据订单流水号 `UNIQUE KEY` 直接查数据库。只要出现该单号，立刻抢救性补偿并重发 `Commit`。即使本地建单真的卡死崩溃了，回查失败 `Rollback` 时我还会附带把刚刚 Redis 扣减出去的假库存给重试加回来，**完美实现了极端异常下的退票回补**。

---

## 🧠 模块二：12306 智能数字员工 (AI Agent / RAG)

### 1. 多工具 ReAct 智能体架构集成
* **Q1: 你的 Agent 只能查票吗？和普通的只做纯文本问答的 RAG 区别在哪？**
  * **答:** 这是信息系统到操作系统级的跃迁。市面开源 RAG 只读不写，而我的大语言模型搭载了通过 `@Tool` 暴露的 Java 底层查票、退单撤销、状态检视三个原生 API。其底层基于 ReAct（推理+行动）范式，一旦捕捉到“这票我不要了”的语义，无需用户点按钮，Agent 层自主推测入参调用退费方法更改数据库状态，是典型的**“读写混合调度型智脑”**。
* **Q2: 解释 Function Calling 的交互与拦截流转。**
  * **答:** 启动时，LangChain4j 扫描 Java 方法签名和注释组装打包发送 LLM 知识库；对话中，若触发命中意图，LLM 会吐出包含调用指引的特定格式 JSON 占位；后端网关拦截到该指令，主动发起 HTTP 代调请求（如查票、订单状态回查、自动退款回补等），最后再将真实的业务反馈结果插穿进原始 Prompt 回传 LLM 进行归纳结语输出。
* **Q3: Java 查票外挂接口缓慢时，大模型单线程会卡死甚至引发 OOM 吗？**
  * **答:** 基于防御性编程，我设计了**请求异步化与流式预加载**；配合原生的 **SSE (Server-Sent Events) 长连接流式预响应**技术。即使退单动作耗费 3 秒，服务端早在一开始便不断推流出诸如“好的亲，已检测到您的指令，正在疯狂为您连入 12306 接口清算余票...”的进度文案，在实现前端绝佳体验的同时斩断了阻塞雪崩。

### 2. Guardrails 安全护栏 & 意图路由
* **Q1: [压轴黑科技] 什么是 Prompt Injection 攻击？你是如何通过 Guardrails 拦截的？**
  * **答:** 攻击者会尝试通过恶意咒语（指令劫持）诱导模型脱离设定。我独辟蹊径地构建了物理层、结构层与 API 层三合一的**纵深防御体系 (Defense in Depth)**：
    1. **物理层 (Guardrails)**：开发了**意图前置的物理安全审查状态机层**。以微米级的极低开销进行黑白双语违禁结构正则或轻量级过滤，识别危词后直接从网关上抛出 `SecurityException` 中止请求，绝不将毒性信息交予大模型，从根本上终结了算力污染与高额 Token 吸费。
    2. **API 隔离层 (Role Separation)**：严格遵循 OpenAI/Anthropic 最佳实践，利用 LangChain4j 将客服人设定义为 `SystemMessage`，用户输入设为 `UserMessage`。利用主流模型原生对齐能力，从底层确立“指令高于输入”的最高权威。
    3. **结构层 (Delimiters)**：在请求发送前，通过 **XML 标签隔离 (Delimiters)** 策略将用户输入包裹在 `<user_input>` 标签内，并附加强制性的“后置防御 Prompt”。这利用了大模型对结构化标签的极强注意力，物理阻断了试图绕过人设的复杂越狱指令。
* **Q2: 意图路由器 (Router) 是如何在毫秒内判定并极大省钱的？**
  * **答:** 通过调用极度轻量与廉价的分类器小模型（或本地嵌入模型），结合结构化约束（强制约束其采用 JSON Schema 仅限回复 CHITCHAT/TICKET/RAG 选项）。凡鉴定为粗口闲聊者立马用系统兜底短语答复返回，直接节约了后续将要引发的高维 Embedding 计算、Milvus 发包与重排算力，帮业务线节省超 80% 本应被无端挥霍的资源成本。
* **Q3: Query Refiner 提问重写不可避免拉长响应延时，该如何权衡？**
  * **答:** 是牺牲响应速度换取极度精准的必然妥协。口语化的长难句提问带入向量库检索其精准命中率几近于 0（都是噪音）。让微小模型用半秒钟时间将其洗练为类似 `[业务要素提取：退票费率+发车前后 24小时档位计算]` 标准查证机器语。这一秒钟的换算抵过了之后 10 秒钟无效幻觉问答对用户的折磨。

### 3. 混合检索 & Cross-Encoder & 白盒单测背书
* **Q1: 什么是 Parent-Child 嵌套切分？解决 RAG 什么致命痛点？**
  * **答:** 在解析复杂费率表或规条时，生硬定长的 500 字一刀切一定会带来“上下文腰斩”或把一个关联表格切分在两片维度的弱智后果。Parent-Child 会把原始完整巨型段落视为背景（Parent）留存隐而不发，但为其生成多个细粒度到 100 字级别的小噪音剥离词块去生成向量用作碰撞触发雷达（Child）。用最微的针探路，拉出的是整张连贯的图纸，彻底解决了 RAG 碎片化上下文引发的大模型脑补式断层（幻觉）。
* **Q2: Dense 检索向量与原生 BM25 稀疏索引的定位分别是什么？**
  * **答:** Dense（Milvus 余弦距离）主攻“泛化理解”与“口语化近似语义映射”（擅长意会）；BM25（纯手工倒排词频与逆文档频次公式）只负责生抠长尾词干与数字编号（专治“G4008 动车违禁品清单”等不能容忍模糊的特殊型号词组），两翼齐飞构成极致互补体系。
* **Q3: 两种分数基准完全割裂，怎么优雅平滑合并并给出最终列表？**
  * **答:** 引入业界鼎牛的 **RRF (倒数等级融合) 算法**。彻底扬弃了毫无意义的绝对打分比较，而直接以它们各自管道下排名论英雄。采用倒数函数 `1 / (常数k + 业务各自Rank名次)` 对上榜条目重新奖励洗牌得分。某文章在两路全高居首位，则因分母极小数导致最终总分极大叠加，这是非常精巧而省内存的统计聚合工程实现。
* **Q4: 如果 Reranker (Cross-Encoder) API 断网超时，系统会挂吗？**
  * **答:** **绝不会。我是典型的防御型程序员。** Reranker 的核心在于将检索出来的双向文本喂进 Transformer 做重重注意力缠绕耗时极大，一旦其网络探针报错，我的异常捕获切片策略会将其短路旁通，直接以次优等级（仅 RRF 产出的前 5 个不精校的备选结果集）作为结果强行降级下发生效。服务不崩盘、可接客才是企业级的第一优先级。
* **Q5: [极佳研发素养] 反复强调“纯白盒单测”比业界通用的黑盒集成联调高级在哪里？**
  * **答:** 行业绝大部分自称开发 RAG 的其实只懂“在代码里发请求接外包服务”，集成测试一碰就死必须全量拉起依赖环境。我的不同在于：我具备手撕核心公式并在内存级阻断 Mock 外部网络的能力，单测中模拟造假分词器数组碰撞验证了 BM25 算法精度、手工硬编码正向夹逼反向死锁矩阵测探余弦值。用毫秒级脱机跑赢单测并全绿通过 CI/CD 构建，这才是后端研发的基本功，而不能总是推诿为环境起不来。

### 4. 双层记忆架构 & Semantic Cache (语义缓存)
* **Q1: 纯粹的 `MessageWindow` 如果溢出会如何？双层记忆网络如何破局？**
  * **答:** 原生滑动窗口超出如 10 条强硬删除时，大模型会表现为立刻“老年失忆症”。在原 Redis 会话层上，**我创新引入了大模型异步钩子驱动的“长时降维池”**：每当前线超格的单条聊天内容即将被丢弃，系统自动发往子线程提取提纯这超长段落提炼为几十位的“事件摘要精华”，追加更新至独立设置 24 时 TTL 的持久化结构里。这保证了即使用户扯皮两万句重口水，只要关键线索都在这最高权重的摘要 SystemMessage 里，它就拥有着超乎想象的续篇长记忆。
* **Q2: 你的 Semantic Cache 为什么要迁移到 Redis 分布式架构？打分机制是什么？**
  * **答:** 这是从 Demo 走向“生产环境集群部署”的必经之路。一开始写在 Java 内存里的 List 或 HashMap 缓存有个致命伤：节点 A 刚花高额 Token 算出来的缓存答案，下一次同样的请求如果是通过 Nginx 负载均衡被打到节点 B 上，那它只能是个瞎子重新去算一遍。**迁移到 Redis Hash 结构后，整个微服务集群的数十个节点就可以共享并快速横向扩容同一个语义蓄水池了。**
    至于机制：首先用 Zhipu 的 Embedding API 将用户的纯净提问转化为高维向量数组，然后使用 Redis (配合 Java 侧或者 RediSearch 的 HNSW 算法) 与内部库进行 Cosine Similarity (余弦相似度内积) 打分。我之所以卡死极严苛的 **0.95 判定阈值的红线**，是因为如果放宽到 0.85，非常容易把"退费 7 天当做 5 天"直接当成是一个语义而误发给客户引发客诉。宁可没命中老老实实走一遍大模型花点钱，也绝不能因为追求命中率而让客服业务产生幻觉误导。
* **Q3: [重灾风险拷问] 修改规章费率后怎么除掉昨日留存在 Redis 里的旧“近似脏数据”缓存？**
  * **答:** 一切由发布操作者联动接管。当后台管理者点击刷新发布全新的 12306 知识文档之时，消息系统立即分叉联动抛发 Webhook 请求。缓存集群在捕捉到指令信号的一毫秒内触发底层硬驱删除接口执行全局级针对 Semantic Cache 的全系毁灭清空（或者对受影响文档前缀的特定清除）。我宁肯牺牲接下来几秒全靠去大模型里老实推理运算的高成本换取这绝对的强数据一致性准绳！
* **Q4: 为什么大模型强迫必须搭配 SSE？WebSockets 不香了吗？**
  * **答:** WebSocket 是给全双工（你一言我一语还要传控制报文）如打游戏专用的。大模型推理吐字的结构具有极为苛刻的：“客户端发送请求之后只能静静接收单向不断从服务端剥开喷发的文字段落”特性。SSE 采用极其简单古朴的 `text/event-stream` 并利用 HTTP 协议标准长挂机制天然对齐模型响应输出。其网络占有开销仅为前者的十分之一且兼容跨域。

### 5. 自动化量化评估 RAGAS (LLM-as-a-Judge)
* **Q1: 相比于人工验证测试，LLM-as-a-Judge 到底颠覆盖特了什么根源痛点？**
  * **答:** 结束了参数调优等于炼丹玄学的盲人摸象式自我欺骗开发期。我不能在调整切片从 500 改为 300 这种重大架构举措后居然没人知道到底是变聪明了还是变傻瓜了！借助于独立的裁判大模型冷面执法自动跑批去校验比对分值，一切性能迭代变得完全数据化，有依可循。
* **Q2: 提供给 RAGAS 评估的 Golden Dataset 数据集什么规模与组成？**
  * **答:** 从系统投用真实场景的脱敏历史工单咨询表里面，手工甄别核对并标定了拥有 100 余条最具代表与陷阱歧义的极具难度的 Q&A 基准库构建作为锚定数据。
* **Q3: RAGAS 量化打分中的 Faithfulness 和 Relevance 是怎么计算的？防范了什么恶习？**
  * **答:** 
    1. **Faithfulness (忠实度)**：用来防备大模型的**幻觉（捏造谎言）**。裁判系统严格判定：生成的最终答复里的哪怕每一个短小的陈述观点，是否有绝对的论据能严格对齐回送回来的“参考片段”。如果他在废话里脑补了退票不要钱，直接判 0 分不忠诚。
    2. **Relevance (相关度)**：用来防备大模型的**答非所问（胡乱引申）**。通过 Cosine Similarity 算法或判决提示词评估系统返回的段落是否真正在正面回答用户提问。这两个维度确立了 AI 回答的商业化落地底线。
* **Q4: 若后续评估发觉 Context Precision 断崖式滑坡，架构层将会怎么针对性修复应对？**
  * **答:** 这种指标坠落核心原因直指我给的知识资料极差。这是我架构底层重构设计的契机：我会马上怀疑是不是我原先所选用的 Embedding 词库存在严重脱离业务常识边界、BM25 分词器词典切分粗糙错误，或者进一步对精选结果加上更为严密逻辑纠偏重构的 Cross-Encoder 精排优化结构，一切以用指标驱动做系统手术。
